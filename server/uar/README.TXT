This is a Unified ARchive. From here you can run the poker system by executing:

mvn firebase:run

This requires that you've first packaged all the modules that this UAR depends
on ("mvn clean install" from the project root).