A very basic website, used for basic infor and user-registration.

This _avoids_ the need to integrate the poker system with a website which 
provides the user-registration (e.g. wordpress).

It is packaged as a war.

BUILD & START
=============

mvn clean package
mvn jetty:run

http://localhost:8089

